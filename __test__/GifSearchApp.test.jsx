import { render, screen } from "@testing-library/react";
import { GifSearchApp } from "../src/GifSearchApp";

describe("first", () => {
  test("Testing", () => {
    render(<GifSearchApp />);
    // screen.debug();
  });
});
